# Legacy v1 forensic tombstone

The legacy v1 runtime is deliberately absent from this tree, the production
wheel, provisioning, and systemd. Its last immutable forensic reference is Git
tag `v0.5.15` at commit `2872f0e6e19298520092fa22a7c98dbb3cb90c6c`.

Do not copy modules or units from that tag into a live host. Historical access
is read-only through Git object storage only.
